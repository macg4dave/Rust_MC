fn main() {
    // Minimal stub for the `make_fakefs` binary so tests can compile the
    // crate even if the real binary source is located elsewhere in the
    // repository. The real make_fakefs used by developers may live in a
    // different path; this keeps test runs fast and deterministic.
    println!("make_fakefs stub (no-op)");
}
