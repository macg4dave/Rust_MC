// Test helpers module (feature = "test-helpers")
// Minimal placeholder to ensure `pub mod test_helpers;` resolves when the
// feature is enabled. Add test helper submodules here as needed.

pub fn _placeholder_test_helper() {}
